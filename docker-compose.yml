version: '3.8'

services:
  evoconnect:
    build:
      context: .
      dockerfile: Dockerfile
    image: evoconnect-dashboard:latest
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
      - BASE_URL=http://75.119.145.128:3000
    volumes:
      - evoconnect_data:/app/data
    deploy:
      restart_policy:
        condition: on-failure

volumes:
  evoconnect_data:
